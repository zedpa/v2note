/**
 * 语音转待办 - 解析器
 * 
 * 🔴 这是骨架代码，所有测试应该失败
 * Claude Code 的任务是让所有测试通过
 * 
 * 对应 Spec：specs/voice-to-todo.md
 * 对应测试：__tests__/features/voice-to-todo.test.ts
 */

import type { ParseVoiceInput, ParseVoiceResult } from './types'

export async function parseVoiceToTodo(
  input: ParseVoiceInput
): Promise<ParseVoiceResult> {
  // TODO: Claude Code 根据 spec 和测试实现此函数
  // 以下是最小骨架，所有测试会失败
  
  return {
    success: false,
    todos: [],
  }
}
