/**
 * Daily cognitive cycle — orchestrates clustering, contradiction scan,
 * promote, and maintenance in sequence.
 */
export declare function runDailyCognitiveCycle(userId: string): Promise<void>;
